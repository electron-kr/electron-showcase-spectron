{
  "name": "electron-spectron-showcase",
  "productName": "ElectronTestWithSpectron",
  "version": "0.0.0",
  "description": "Show how to use spectron with ava on electron for testing",
  "license": "MIT",
  "repository": "ragingwind/electron-spectron-showcase",
  "author": {
    "name": "ragingwind",
    "email": "ragingwind@gmail.com",
    "url": "ragingwind.me"
  },
  "electronVersion": "0.36.0",
  "scripts": {
    "test": "xo && ava --serial",
    "start": "electron .",
    "build": "electron-packager . $npm_package_productName --out=dist --ignore='^/dist$' --prune --asar --platform=darwin --arch=x64 --version=$npm_package_electronVersion"
  },
  "files": [
    "index.js",
    "index.html",
    "index.css"
  ],
  "keywords": [
    "electron-app",
    "electron"
  ],
  "dependencies": {
    "electron-debug": "^0.5.0"
  },
  "devDependencies": {
    "ava": "^0.9.1",
    "electron-bin-path": "^0.1.0",
    "electron-packager": "^5.0.0",
    "electron-prebuilt": "^0.36.0",
    "spectron": "^0.36.0",
    "xo": "^0.12.1"
  },
  "xo": {
    "esnext": true,
    "envs": [
      "node",
      "browser"
    ]
  }
}
